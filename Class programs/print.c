#include <stdio.h> // stdio - standard input and output, .h - header file
int main() // use int as return type for main function as linux compiler does not support void
{
	printf("My first C program");
	return 0; // int is return type of main function
}